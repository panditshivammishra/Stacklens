[pytest]
# asyncio_mode = auto: every `async def test_...` runs automatically, without
# needing an @pytest.mark.asyncio decorator on each one.
asyncio_mode = auto
testpaths = tests
addopts = -v --tb=short
markers =
    slow: hits a real external API (LLM/embeddings) — costs quota. Skip with -m "not slow"
